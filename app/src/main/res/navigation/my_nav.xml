<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.study.quizzler.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/historyPage"
        android:name="com.study.quizzler.activities.HistoryPage"
        android:label="activity_history_page"
        tools:layout="@layout/activity_history_page" />
    <activity
        android:id="@+id/aboutUs"
        android:name="com.study.quizzler.activities.AboutUs"
        android:label="activity_about_us"
        tools:layout="@layout/activity_about_us" />
    <activity
        android:id="@+id/settingsPage"
        android:name="com.study.quizzler.activities.SettingsPage"
        android:label="activity_settings_page"
        tools:layout="@layout/activity_settings_page" />
</navigation>